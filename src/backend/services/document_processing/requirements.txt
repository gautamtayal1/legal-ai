# Text Extraction Dependencies (simplified)
PyPDF2==3.0.1
pdfplumber==0.10.3
python-docx==1.1.0
chardet==5.2.0

# OCR Dependencies (tesseract called directly)
pillow==10.1.0
pymupdf==1.23.26

# Vector Databases (for other pipeline steps)
pinecone-client==2.2.4
weaviate-client==3.25.3
chromadb==0.4.18
qdrant-client==1.7.0

# Search Engines (for other pipeline steps)
elasticsearch==8.11.1
opensearch-py==2.4.2

# Embedding Models (for other pipeline steps)
openai==1.3.8
cohere==4.37

# Background Jobs
celery==5.3.4
redis==5.0.1
rq==1.15.1

# Utilities (only what we actually use)
python-dateutil==2.8.2

# PostgreSQL Vector Extension
pgvector==0.2.4
psycopg2-binary==2.9.9

# Monitoring and Logging
structlog==23.2.0
prometheus-client==0.19.0 